def solution(array, commands):
    answer = []
    for i in range(len(commands)):
        i, j, k = commands[i]
        arr = array[i-1 : j]
        arr.sort()
        answer.append(arr[k-1])
        
    return answer